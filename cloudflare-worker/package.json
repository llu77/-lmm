{
  "name": "hello-world-worker",
  "version": "1.0.0",
  "description": "Simple Cloudflare Worker that responds with Hello World",
  "main": "index.ts",
  "scripts": {
    "build": "echo 'No build needed for hello-world-worker'",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "keywords": ["cloudflare", "workers", "hello-world"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251121.0"
  }
}
