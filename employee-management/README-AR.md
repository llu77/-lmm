# نظام إدارة الموظفين - Employee Management System

## 📋 نظرة عامة

نظام متكامل لإدارة بيانات الموظفين مبني على Strapi CMS. يوفر لوحة تحكم شاملة باللغة العربية لإدارة كافة معلومات الموظفين.

## ✨ المميزات

### معلومات الموظف الشخصية
- رقم الموظف (فريد)
- الاسم الأول والأخير
- الاسم الكامل بالعربية
- البريد الإلكتروني
- رقم الهاتف
- رقم الهوية الوطنية
- تاريخ الميلاد
- النوع (ذكر/أنثى)
- العنوان الكامل (المدينة، الدولة)

### معلومات التوظيف
- المسمى الوظيفي
- القسم (الإدارة، الموارد البشرية، تقنية المعلومات، المالية، المبيعات، التسويق، العمليات، الدعم)
- تاريخ التعيين
- حالة التوظيف (نشط، في إجازة، موقوف، منتهي)
- الراتب
- نوع العقد (دوام كامل، دوام جزئي، عقد، متدرب)

### معلومات إضافية
- جهة الاتصال في حالات الطوارئ
- صورة الموظف
- المستندات المرفقة
- ملاحظات
- حالة النشاط

## 🚀 التثبيت والتشغيل

### المتطلبات
- Node.js (v18 أو أحدث)
- npm أو yarn

### خطوات التشغيل

1. **الانتقال إلى مجلد المشروع:**
```bash
cd employee-management
```

2. **تثبيت المكتبات:**
```bash
npm install
```

3. **تشغيل الخادم في وضع التطوير:**
```bash
npm run develop
```

4. **الوصول إلى لوحة التحكم:**
   - افتح المتصفح على: `http://localhost:1337/admin`
   - أنشئ حساب المسؤول الأول
   - ابدأ بإدارة الموظفين

## 📁 هيكل المشروع

```
employee-management/
├── src/
│   ├── admin/           # تكوين لوحة التحكم
│   │   └── app.tsx     # دعم اللغة العربية
│   ├── api/
│   │   └── employee/   # API الموظفين
│   │       ├── content-types/
│   │       │   └── employee/
│   │       │       └── schema.json
│   │       ├── controllers/
│   │       │   └── employee.ts
│   │       ├── services/
│   │       │   └── employee.ts
│   │       └── routes/
│   │           ├── employee.ts
│   │           └── custom-employee.ts
│   └── index.ts
├── config/             # إعدادات التطبيق
├── database/          # قاعدة البيانات
└── public/            # الملفات العامة
```

## 🔌 API Endpoints

### نقاط الوصول الأساسية

- `GET /api/employees` - جلب جميع الموظفين
- `GET /api/employees/:id` - جلب موظف محدد
- `POST /api/employees` - إضافة موظف جديد
- `PUT /api/employees/:id` - تحديث بيانات موظف
- `DELETE /api/employees/:id` - حذف موظف

### نقاط وصول مخصصة

- `GET /api/employees/active` - جلب الموظفين النشطين فقط
- `GET /api/employees/department/:department` - جلب موظفي قسم معين

### مثال على طلب API

```bash
# جلب جميع الموظفين
curl http://localhost:1337/api/employees

# إضافة موظف جديد
curl -X POST http://localhost:1337/api/employees \
  -H "Content-Type: application/json" \
  -d '{
    "data": {
      "employeeId": "EMP001",
      "firstName": "أحمد",
      "lastName": "محمد",
      "email": "ahmed@example.com",
      "phone": "+966501234567",
      "position": "مطور برمجيات",
      "department": "it",
      "hireDate": "2024-01-15",
      "salary": 8000,
      "contractType": "full_time",
      "employmentStatus": "active"
    }
  }'
```

## 🎨 لوحة التحكم

### المميزات
- واجهة باللغة العربية
- إدارة شاملة للموظفين
- رفع الصور والمستندات
- بحث وفلترة متقدمة
- تصدير البيانات
- إدارة الصلاحيات

### الأقسام المتاحة

1. **إدارة المحتوى**
   - قائمة الموظفين
   - إضافة موظف جديد
   - تعديل بيانات الموظفين
   - حذف الموظفين

2. **المكتبة الإعلامية**
   - صور الموظفين
   - المستندات المرفقة

3. **الإعدادات**
   - إدارة المستخدمين
   - الصلاحيات والأدوار
   - إعدادات API

## 🔒 الأمان والصلاحيات

يدعم النظام إدارة متقدمة للصلاحيات:
- دور المسؤول الرئيسي (Super Admin)
- دور مدير الموارد البشرية (HR Manager)
- دور الموظف العادي (Employee)

يمكن تخصيص الصلاحيات من خلال:
Settings → Roles → Edit Role

## 📊 التقارير والإحصائيات

يوفر النظام خدمات مخصصة للحصول على:
- إحصائيات الموظفين حسب القسم
- حساب إجمالي الرواتب
- عدد الموظفين النشطين
- توزيع الموظفين حسب نوع العقد

## 🛠️ أوامر NPM المتاحة

```bash
# تشغيل وضع التطوير
npm run develop

# تشغيل وضع الإنتاج
npm run start

# بناء لوحة التحكم
npm run build

# إنشاء مسؤول جديد
npm run strapi admin:create-user

# تصدير البيانات
npm run strapi export
```

## 🌐 قاعدة البيانات

يستخدم النظام SQLite بشكل افتراضي للتطوير. يمكن تغييره إلى:
- PostgreSQL
- MySQL
- MariaDB
- MongoDB

من خلال ملف `config/database.ts`

## 📝 الترجمة

النظام يدعم اللغة العربية بشكل كامل في:
- واجهة لوحة التحكم
- رسائل النظام
- التحقق من البيانات
- الإشعارات

## 🤝 المساهمة

لتحسين النظام:
1. قم بعمل Fork للمشروع
2. أنشئ فرع للميزة الجديدة
3. قم بتنفيذ التغييرات
4. أرسل Pull Request

## 📄 الترخيص

MIT License

## 📞 الدعم الفني

للمساعدة والاستفسارات:
- الوثائق الرسمية: https://docs.strapi.io
- المجتمع: https://strapi.io/community

---

**تم التطوير بواسطة:** Claude AI
**الإصدار:** 1.0.0
**آخر تحديث:** 2025
