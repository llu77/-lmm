{
  "name": "symbolai-root",
  "version": "1.0.0",
  "scripts": {
    "build": "cd symbolai-worker && npm install && npm run build",
    "dev": "cd symbolai-worker && npm run dev",
    "deploy": "cd symbolai-worker && npm run deploy"
  },
  "dependencies": {
    "@astrojs/react": "^4.4.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "wrangler": "^4.46.0"
  }
}
